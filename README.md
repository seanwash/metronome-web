# metronome.seanwash.com

Just a metronome.

## Notes (to self)

* Hosted on DigitalOcean via docker (compose, see `/docker`).
* Deploys are handled via sftp.
* `release.sh` can be used to update the running container.
